import classNames from "./Chat.module.scss";

const Chat = () => {
    return (
        <section className={classNames.chat}>
            тут будет текущий чат
        </section>
    );
}

export { Chat };