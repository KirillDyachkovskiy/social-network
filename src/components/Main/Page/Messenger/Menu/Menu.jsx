import classNames from "./Menu.module.scss";

const Menu = () => {
    return (
        <section className={classNames.chat}>
            тут будет текущий чат
        </section>
    );
}

export { Menu };